export default function Loading() {
  return (
    <main className="mx-auto mt-20 w-full max-w-xl rounded-lg border border-border bg-surface p-6">
      <p className="font-display text-lg text-primary">Carregando interface...</p>
    </main>
  );
}
